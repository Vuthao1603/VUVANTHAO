# Bỏ qua node_modules (thư viện cài đặt)
node_modules/

# Bỏ qua file cấu hình chứa thông tin database, API keys
.env
config/*.env
config/database.js

# Bỏ qua thư mục build (nếu có)
dist/
build/

# Bỏ qua file log và cache
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
